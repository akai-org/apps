---
import { type Project } from "../utilities/data";

interface Props extends Project {}

const { name, url, languages, stargazerCount } = Astro.props;
---

<div class="w-5/6 rounded-md border-2 border-gray-100 p-3">
  <h2 class="text-xl font-bold">{name}</h2>
  <span>{stargazerCount} gwiazdek</span>
  <ul class="flex flex-row flex-wrap gap-2 text-sm">
    {languages.map((language) => <li>{language}</li>)}
  </ul>
  <a href={url} class="text-yellow-500">Sprawdź</a>
</div>
